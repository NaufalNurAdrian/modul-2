const nextConfig = {
    /* config options here */
    images: {
      remotePatterns: [
        {
          hostname: 'image.uniqlo.com',
        },
      ],
    },
  };
  
  export default nextConfig;